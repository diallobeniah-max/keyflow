/// Rust engine placeholder.
/// The TypeScript engine powers browser mode now. This module should mirror the
/// same state machine for the desktop build so hooks can run even when the UI is hidden.
pub struct ShortcutEngine;
impl ShortcutEngine { pub fn new() -> Self { Self } }
